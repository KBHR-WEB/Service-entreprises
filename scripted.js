function toggleMenu() {
    var sideMenu = document.querySelector('.side-menu');
    sideMenu.style.width = sideMenu.style.width === '75px' ? '0' : '75px';
  }
  
  
function closeMenu() {
    var sideMenu = document.querySelector('.side-menu');
    sideMenu.style.width = '0';
  }
  
